<h3>Ergebnisse der stationären Simulation</h3>

<p>Die Ergebnisse der stationären Simulation werden in Form einer Tabelle angezeigt. Werden mehrere Szenarios miteinander verglichen, werden Unterschiede gegenüber Szenario 1 (am weitesten links stehende Spalte) durch die Farbe des Zellhintergrunds hervorgehoben. Hat sich der Gleichgewichts-Werte einer Zustandsvariable im Szenario 'i' (i > 1) gegenüber Szenario 1 erhöht, erscheint die Zelle in orange, hat sich der Wert vermindert, erscheint die Zelle in blau.</p>

<h3>Wenn kein Gleichgewichtszustand existiert</h3>

<p>Die Existenz eines Gleichgewichtszustands (oder mehrerer solcher Zustände) ist abhängig von den Modellgleichungen, der Parametrisierung und ggf. auch von den Anfangswerten der Zustandsvariablen. Es kann vorkommen, das die Werte der Zustandsvariablen trotz Integration über einen sehr langen Zeitraum nicht gegen einen bestimmten Wert konvergieren. Das System besitzt dann für die gegebene Parametrisierung keinen Gleichgewichtszustand. In diesem Fall sind in der ausgegebenen Tabelle alle Werte auf "NA" gesetzt.</p>

<p>Schlägt die Berechnung einer Gleichgewichtslösung unerwartet fehl, sollte zunächst eine dynamische Simulation durchgeführt werden. Wird die Dynamik über einein längeren Zeitraum betrachtet, zeigt sich, welche der Zustandsvariablen nicht gegen einen festen Wert konvergiert.</p>
